#!/bin/bash
set -e

DIR="$( cd "$( dirname "${BASH_SOURCE[0]}" )" >/dev/null 2>&1 && basename `pwd` )"
echo THIS IS A FAKE REAL USED FOR TESTS > rsl.out.0000
cat namelist.input >> rsl.out.0000
echo wrfbdy_d01 `cat namelist.input` $DIR > wrfbdy_d01
